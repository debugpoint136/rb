/**
 * ===BASE===// app // launchappPanel.js
 * @param 
 */

function launchappPanel(event) {
    menu_shutup();
    menu.apppanel.style.display = 'block';
    menu_show_beneathdom(17, event.target);
//menu.apppanel.kwinput.focus();
}
