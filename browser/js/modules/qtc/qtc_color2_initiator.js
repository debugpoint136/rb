/**
 * ===BASE===// qtc // qtc_color2_initiator.js
 * @param 
 */

function qtc_color2_initiator(event) {
    paletteshow(event.clientX, event.clientY, 31);
    palettegrove_paint(event.target.style.backgroundColor);
}
