/**
 * ===BASE===// palette // tk_barplotbg_initiator.js
 * @param 
 */

function tk_barplotbg_initiator(event) {
    paletteshow(event.clientX, event.clientY, 43);
    palettegrove_paint(menu.c44.color.style.backgroundColor);
}
