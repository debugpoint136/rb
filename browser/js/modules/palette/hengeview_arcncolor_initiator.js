/**
 * ===BASE===// palette // hengeview_arcncolor_initiator.js
 * @param 
 */

function hengeview_arcncolor_initiator(event) {
    paletteshow(event.clientX, event.clientY, 11);
    palettegrove_paint(event.target.style.backgroundColor);
}
