/**
 * ===BASE===// palette // tk_bgcolor_initiator.js
 * @param 
 */

function tk_bgcolor_initiator(event) {
    paletteshow(event.clientX, event.clientY, 15);
    palettegrove_paint(menu.c44.color.style.backgroundColor);
}
