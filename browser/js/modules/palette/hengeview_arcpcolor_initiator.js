/**
 * ===BASE===// palette // hengeview_arcpcolor_initiator.js
 * @param 
 */

function hengeview_arcpcolor_initiator(event) {
    paletteshow(event.clientX, event.clientY, 10);
    palettegrove_paint(event.target.style.backgroundColor);
}
